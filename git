The git plugin provides fundamental git operations for Jenkins projects.
It can poll, fetch, checkout, branch, list, merge, tag, and push repositories ·
Release notes are recorded in GitHub Releases since July 1, 2019 (git plugin 3.10.1 and later).
Prior release notes are recorded in the git ...


hiiiiiii
git is nothing but global information tracker
git is used for track the files
git is paltform independent
git is version control system
